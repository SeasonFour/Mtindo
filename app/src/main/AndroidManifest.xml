<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hulk.mtindo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">

        <!--
        <activity android:name=".Mtindo"
            android:label="@string/app_name"
            android:theme="@style/MyMaterialTheme" >

        </activity>
        -->

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MyMaterialTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Post_update" ></activity>
        <activity
            android:name=".Login"
            android:theme="@style/MyMaterialTheme"></activity>
        <activity
            android:name=".Createstore"
            android:theme="@style/MyMaterialTheme"></activity>
        <activity android:name=".Hair" ></activity>

        <activity
            android:name=".Beauty_pros"
            android:label="@string/title_activity_beauty_pros"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
            android:theme="@style/MyMaterialTheme" />
        <activity android:name=".Makeup">

        </activity>
        <activity android:name=".MyAppIntro">

        <!--<activity android:name=".OffersSlider">-->

        <!--</activity>-->
        <activity android:name=".SplashScreen">

        </activity>
    </application>

</manifest>
